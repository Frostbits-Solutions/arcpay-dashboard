{
  "name": "arcpay",
  "private": "true",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "gen-types": "supabase gen types typescript --project-id 'tzteqywipcdkdgnijbnu' --schema public > packages/supabase/src/database.types.ts"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "npm@11.6.1",
  "engines": {
    "node": ">=22"
  }
}
